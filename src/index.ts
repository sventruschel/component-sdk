
/**
 * Prefab generates a prefab JSON structure
 * 
 * more information [here](http://example.com)
 * @param message a message to be logged
 * @returns 
 */
export const prefab = (message: string) => console.log(message)